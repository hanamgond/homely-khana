import ContactClient from '@/components/dashboard/ContactClient';

export const metadata = { title: 'Contact Us - HomelyKhana' };

export default function ContactPage() {
    return <ContactClient />;
}